{% extends "documentation/reference/component" %}

{% block componentDetails %}
	<p>
		Pebble provides the following comparison operators: ==, !=, <, >, <=, >=. 
		All of them except for <code>==</code> are equivalent to their Java counterparts. The <code>==</code>
		operator uses <code>java.util.Objects.equals(a, b)</code> behind the scenes
		to perform null safe value comparisons. <code>equals</code> is an alias for <code>==</code>.
	</p>

	<p>
		<pre><code>{{ 
'
{% if user.name equals "Mitchell" %}
	...
{% endif %}
'
		| trim | escape }}</code></pre>
	</p>
{% endblock componentDetails %}
	